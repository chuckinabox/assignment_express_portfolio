<h1>Chuck's projects</h1>

{{#each projects as |project|}}
  <div class="project">
    <div class="project-name">
      <a href="/{{project.link}}">
         {{project.name}}
        <img src={{project.imagePath}}>
      </a>
      <br>
      <br>

    </div>
  </div>
{{/each}}
